services:
  - type: web
    name: rentwithmsbackend
    runtime: node
    buildCommand: cd backend && npm install && cd ..
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: rentwithmsdb
          property: connectionString
